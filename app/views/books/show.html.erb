<p id="notice"></p>
  <div class="container">
  <div class="row">
   <div class="col-md-3">
 <%= render 'side', user: @user, book: @book_new %>
   </div>

<div class= "col-md-8 offset-md-1">
  <h2>Book detail</h2>
  <table class="table">
    <tbody>
      <tr>
        <td>
          <%= image_tag @user.get_profile_image(100,100) %>
          <br>
          <%= link_to current_user.name, user_path(current_user) %>
          </br>
        </td>
        <td>
          <%= link_to @book.user_id, book_path(params[:id]) %>
        </td>
        <td> <%= @book.body %> </td>
        <td> <%= link_to 'edit', edit_book_path %></td>


      </tr>
     </tbody>
    </table>
  </div>
 </div>
</div>



